@app/routes/app.dashboard/api/dev/video-compress-compare/batch 这块在现有服务器上跑特别慢，很影响测试效率，现在有两个想法不知道好不好：
1.把ffpeg拿属性这块给它拆成单独的服务，可以部署到别的机器上，通过env可以配置是否开启ffpeg测试计算服务，并配置访问链接(没有或链接失败fallback到本机)
2.这个vmaf测试很慢，它是否支持gpu加速?我如果把它拆到单独的服务里，然后运行在我家的电脑上（i78700k + 64G + 2080ti 22g）不知道能不能快点。或者多并行几个任务