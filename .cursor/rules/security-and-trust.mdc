---
description: Worker 鉴权、URL 校验与日志
globs:
  - "src/**/*.ts"
alwaysApply: false
---

# 安全与可信数据

HTTP 分层见 `worker-boundaries.mdc`。真源：[configuration.md](../../docs/AI与人类/configuration.md) · [decisions.md](../../docs/AI与人类/decisions.md) D12。

## 必须执行

- 所有入站请求体、Header、query 视为不可信。
- **主安全边界**：`Authorization: Bearer` 共享密钥；部署 **HTTPS**（反代/Tailscale）。
- job spec 中视频 URL 仅允许 **`https:`** scheme + 基本 URL 校验；拒绝 `http:`、`file:`、`ftp:` 等。
- **不做** Shopify CDN 域名白名单（BFF 为唯一调用方，Bearer 鉴权即可）。
- 输入须运行时 guard；禁止 `as` 断言后直接信。
- 禁止 log/返回 token、R2 secret、带 query 的完整 CDN URL、堆栈。

| 反例 | 正例 |
|------|------|
| 无 token 公网暴露 | HTTPS + Bearer |
| `fetch("http://…")` | 拒绝非 https |
| `log.info({ url })` 含 signed query | `log.info({ jobId, urlHost })` |

## 默认执行

- 401 不泄露 job 是否存在。
- dev-only；不得替代 production 商家路径内嵌 ffmpeg。

## 日志

- pino 结构化；字段 `jobId`、`phase`、`durationMs`。
