---
description: TypeScript 通用可读性、注释和调试友好规则
globs:
  - "src/**/*.ts"
alwaysApply: false
---

# 代码风格（共用）

本规则强调显式、低歧义和方便断点调试。服务端分区细则见 `code-style.server.mdc`。

## 默认执行

- 默认使用具名 `function` 声明或 function expression，便于识别调用栈和设置断点。
- 复杂分支使用 `if`、`switch` 或 IIFE 显式展开。
- `if`、`else`、循环和 `catch` 使用花括号并换行。
- 高阶函数中的复杂回调换行并显式 `return`；复杂回调提取为具名函数。
- 文件头说明模块职责；导出函数、导出类型和关键业务约束使用 JSDoc。
- 注释解释意图、原因、边界与异常情况，避免仅复述代码语法。

## 允许的简化

- 简单单层三元允许使用；禁止嵌套三元或在三元中隐藏副作用。
- 简短、无副作用且一眼可读的数组转换、过滤等回调允许使用箭头函数和表达式返回。
- 名称已经完整表达含义的内部字段、简单形参和显然分支，不要求重复注释。
- 无副作用的简单提前返回可以保持紧凑，但同一代码块内应保持一致。

## 必须执行

- 禁止在 `&&`、三元、逗号表达式等紧凑语法中隐藏业务副作用。
- 禁止无意义缩写和无法从上下文理解的名称。
- job 状态字段与 phase 枚举须在类型或 JSDoc 中说明语义。
- 特殊 `switch case`、贯穿逻辑和非显然分支必须注释原因。
- 新建 `*.ts` 模块须有文件头「模块名称 / 模块说明」注释块。

示例见 `examples/readable-code.example.ts`。
