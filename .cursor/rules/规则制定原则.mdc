---
description: SlimVID 规则制定与冲突裁决的元原则
alwaysApply: true
---

# 规则制定原则

此文件用于解释和裁决 `.cursor/rules/` 中的开发规则，不替代具体领域规则。

## 最高目标

所有规则最终服务于：让维护者更快理解、定位、调试并安全修改代码。

- 优先降低人的认知与调试成本，不追求最短代码。
- 显式表达通常优于隐藏分支、副作用和隐式约定。
- 一致性很重要，但不能反过来降低局部可读性。
- 注释解释意图、原因、约束和异常情况，不只翻译代码。
- 抽象必须减少真实复杂度或形成稳定边界，不为减少行数而抽象。
- 修改应聚焦当前任务，不顺手重构、格式化或补完无关功能。

## Think Before Coding（编码前先想清楚）

约束 Agent **何时停、说什么、问什么**，减少默默假设、藏起困惑、在歧义任务上直接开写。具体架构边界仍以 `codingRole.mdc` 及各领域 mdc 为准。

**与「更短实现」的裁决：** 若更少行数与可调试性、显式分支、分层边界冲突，以本项目可维护性与架构规则为准。

### 必须停下来

出现以下任一情况，**先澄清或呈现选项，再写代码**：

- 需求有多种合理解读（例如「修 job」可能指 HTTP handler、job store、ffmpeg 编排或主 app DTO 对齐）。
- 涉及协议或语义未写清（cancel 终态、partial report、poll 行为、fallback 条件）。
- 改动跨层但用户未指明落点（新字段该进 job spec、status 响应还是 domain report）。
- 存在明显更简单的替代路径（用户要求新抽象，但现有模块已有同类实现）。
- 可能与 `docs/` 或主 app `dev-video-compress-compare` 联调结论冲突。
- 安全边界不清（**https URL 校验**、Bearer 鉴权、是否允许 SSRF）。
- 成功标准模糊（「优化一下」「改好点」没有可验证检查项）。

明显 trivial 任务（错别字、单行 import、用户已给出精确文件与行号）可跳过下列准备，但仍不得扩大改动范围。

### 编码前最小准备

非 trivial 任务在动手前完成：

1. **读上下文**：相关模块、主 app 对照实现、已有 `.cursor/rules/examples/` 与领域 mdc；触及 `docs/` 时先读已有 harness，勿臆造字段。
2. **显式列出假设**（不确定的标注「待确认」）：用户意图的一句话复述；改动落点（HTTP handler / job store / domain / ffmpeg）；是否触及 `testing-and-quality.mdc` 中的高风险契约。
3. **有歧义时呈现选项**，不默默选一个：每选项说明做法、影响范围、与现有规则是否一致、主要风险；给出推荐及理由；若推荐违背用户字面表述，必须说明。
4. **有 tradeoff 时必须说出来**：复杂度 vs 可调试性（本项目偏后者）；本次范围 vs 顺带修遗留；远程 worker vs 主 app 本机 fallback 语义。

### 主动 push back

以下情况应指出更简单或更安全的做法，而不是迎合字面需求：

- 方案违反分层或安全边界（handler 内 spawn ffmpeg、拉取非 **https** URL 等）。
- 为单次使用新建抽象、配置层或「预留扩展」。
- 方案会扩大 diff（顺手重构、统一风格、删除未要求处理的遗留代码）。
- 协议语义未定时先写实现再「到时候再对齐」。

表述格式：**「更简单或更安全的做法是 X；若仍坚持 Y，影响是 Z」**。

### 向用户提问

- 一次问**阻塞实现**的关键点；分层、typecheck、test 等常识无需向用户确认。
- 问题要**可回答**：给选项或建议默认，避免开放式「你想要怎样？」。
- 文档与代码能自行核实的（现有函数、路由、类型定义），先查再问。

### 多步任务计划

与 `testing-and-quality.mdc` 衔接，用可验证步骤陈述计划：

```
1. [步骤] → verify: [typecheck / lint / test / 手动检查点]
2. [步骤] → verify: [...]
```

成功标准须具体（例如「POST /jobs 契约测试通过且 typecheck 通过」），不得使用「弄好就行」类弱标准。

## 规则等级

### 必须执行

涉及安全、身份、权限、资源归属、跨层依赖、类型边界、副作用归属和质量门禁的规则。不得因个人风格偏好绕过。

### 默认执行

涉及目录组织、函数写法、组件拆分和编排模式的规则。只有在默认写法明显增加认知成本时才允许偏离，并应与周围代码保持一致。

### 推荐实践

涉及局部展示、注释详细程度和简单表达式写法的规则。以让当前代码更容易阅读和调试为判断标准。

## 冲突裁决

规则冲突时按以下顺序裁决：

1. 安全、身份与数据正确性。
2. 架构边界和副作用归属。
3. 可测试性与可观测性。
4. 人类阅读与调试效率。
5. 局部风格统一。

若偏离默认规则，应保证原因可以从代码或短注释中理解，不创建隐含约定。
